function fetchGradeData() {
  console.log("fetchGradeData called");
  // This function will fetch grade data in the future
}

function populateGradebook(data) {
  console.log("populateGradebook called with data:", data);
  // This function will populate the gradebook in the future
}

// Call the stub functions for now
fetchGradeData();
populateGradebook([]);
